os = type:str parent:'os'
mount = type:str
threshold = type:int  default:100 descr:'Size in mega before the autoscale should add new disk'
incrementSize = type:int default:1000 descr:'Incremental disk size'
