process.1                      =
    args:,
    cmd:'autossh -o "StrictHostKeyChecking no" -NR $(remote.bind):$(remote.port):$(local.address):$(local.port) $(remote.login)@$(remote.address) -p $(remote.connection.port)',
    cwd:,
    filterstr:'$(remote.bind):$(remote.port):$(local.address):$(local.port) $(remote.login)@$(remote.address)',
    ports:,
    prio:'1',
    startupmanager:'upstart',
    timeout_start:'10',
    timeout_stop:'10',

ubuntu.packages                =
    'autossh',

